# 업데이트 안내 — v2

**터미널에 한 줄, claude 껐다 켜기, 파트너에게 한 문장.** 이게 전부입니다.
5분이면 끝나고, 그동안 쌓은 기록은 하나도 지워지지 않습니다.

> 지금 쓰는 판은 `VERSION` 파일에 적혀 있습니다. 지난 판들이 뭘 바꿨는지는 `CHANGELOG.md` 에 있습니다.

---

## 1. 이번 판이 바꾼 것

| 무엇 | 어떤 게 달라지나 |
|---|---|
| **일정을 말로 바꿀 수 있게 됐습니다** | "금요일 2시에 A업체 미팅 잡아줘", "3시 미팅 4시로 옮겨줘", "그거 취소해줘" 가 이제 됩니다. 전에는 화면에서 직접 눌러야 했습니다 |
| **구글 캘린더 연결이 중간에 막히던 문제** | 연결하다가 멈춰 버리는 경우가 있었습니다. 고쳤습니다 |
| **지난 대화가 화면에 보입니다** | 오른쪽 아래 말풍선(파트너와 대화)을 열면 전에 나눈 얘기가 그대로 있습니다. 지금까지는 늘 비어 있었는데, 저희 쪽 버그였습니다 |
| **업데이트가 쉬워집니다** | 다음 판부터는 지금 이 안내보다 더 짧아집니다 |

### 이번 판에 **없는** 것

- **텔레그램과 화면의 대화가 하나로 합쳐지는 것** — 아직입니다. 지금은 여전히 따로 흐릅니다.
- **창을 닫아도 파트너가 계속 살아 있는 것** — 아직입니다. 받는 창은 계속 열어 두셔야 합니다.

둘 다 설계는 끝났고 **다음 판** 목표입니다. 이번 판에는 들어 있지 않습니다.

---

## 2. 업데이트하는 법

### 1단계 · 터미널에 이 한 줄

터미널을 열고 **파트너 폴더로 이동**한 다음(폴더 경로는 각자 다릅니다):

```
cd ~/Downloads/my-biz-partner
```

> 경로가 헷갈리면 `cd ` 까지만 치고 **Finder 의 파트너 폴더를 터미널 창에 끌어다 놓으면** 경로가 저절로 입력됩니다.

그리고 아래를 **통째로 복사해서** 붙여넣고 엔터:

```bash
[ -f ./install.sh ] || { echo "파트너 폴더가 아니에요"; exit 1; }
curl -fsSL https://github.com/hminnnnnn/my-biz-partner/archive/refs/heads/main.tar.gz \
| tar -xz --strip-components=1 \
  --exclude '*/identity/IDENTITY.md' \
  --exclude '*/identity/avatars/*.svg' \
  --exclude '*/notes/inbox/*' --exclude '*/notes/issues/*' --exclude '*/notes/projects/*' \
  --exclude '*/notes/calendar-fallback.md' \
  --exclude '*/state/*' \
  --exclude '*/_import/*' \
  --exclude '*/.claude/settings.json' \
  --exclude '*/.claude/settings.local.json'
echo "다 됐어요. claude 를 껐다 켠 뒤 '업데이트 마무리해줘' 라고 하세요."
```

> **이게 보이면 성공** — 마지막 줄에 `다 됐어요. claude 를 껐다 켠 뒤 …` 가 뜹니다.
> 대신 **`파트너 폴더가 아니에요`** 가 뜨면 다른 폴더에 있는 것입니다. `cd` 부터 다시 하세요.
> (엉뚱한 폴더를 덮어쓰지 않게 막아 주는 장치라, 이 줄은 지우지 마세요.)

### 2단계 · claude 를 껐다 켜 주세요 ★

**이 단계를 빼먹으면 다음 단계가 안 됩니다.**

새로 도착한 기능은 claude 를 **다시 켜야** 인식됩니다. 켜져 있는 창에서 `Ctrl+C` 나 `/exit` 로 끄고,
같은 폴더에서 `claude` 를 다시 실행하세요. **텔레그램 받는 창도 같이** 껐다 켜 주세요.

### 3단계 · 파트너에게 한 문장

```
업데이트 마무리해줘.
UPDATE.md 부터 읽고, 내가 고친 게 있으면 먼저 알려줘.
```

파트너가 남은 뒷정리를 하고, **내가 손댔던 부분이 있으면 사람 말로 알려 줍니다.**
(예: *"권한 목록에 ○○ 를 추가하셨더라고요. 그대로 유지했어요."*)

> **이게 보이면 성공** — 파트너가 무엇이 바뀌었는지 몇 줄로 정리해 주고, 물어볼 게 있으면 물어봅니다.
> 그다음 텔레그램에 **"내일 일정 브리핑해줘"** 를 한 번 보내 답이 오면 끝난 것입니다.

---

## 3. 내 것은 지워지지 않습니다

### 그대로 남는 것

| 무엇 | 어디 |
|---|---|
| 그동안 쌓은 **기록 전부** | `notes/` — `inbox/`·`issues/` 는 물론 **내가 만든 폴더도 전부** |
| **나를 소개한 내용** | `identity/IDENTITY.md` |
| 할 일 · 팀 진행현황 · 자동화 설정 · 활동 기록 | `state/` 폴더 전체 |
| **연결해 둔 구글 캘린더** | 다시 로그인하지 않아도 됩니다 |
| 아직 정리 안 한 **들여오기 원본** | `_import/` |
| **내가 직접 만든 기능(스킬)** | `.claude/skills/` 안의 내가 만든 폴더 |
| 내가 만든 아바타 그림 | `identity/avatars/` |
| **텔레그램 연결** | 파트너 폴더 **밖**에 있어서 업데이트가 아예 스치지 않습니다 |

### 새 판으로 바뀌는 것

`CLAUDE.md`(파트너의 행동 지침) · `notes/README.md`(폴더 규약) · `.claude/settings.json`(권한 목록).

**여기를 직접 고치셨다면 파트너가 3단계에서 알려 줍니다.** 그때 "다시 넣어줘"라고 하면 됩니다.
(대부분은 손댈 일이 없는 파일이라, 조용히 지나갈 겁니다.)

---

## 4. 되돌리는 법

**되돌릴 일이 거의 없습니다.** 업데이트는 기록·설정을 건드리지 않고, 새 파일만 얹습니다.

그래도 뭔가 이상하면 — **파트너에게 말하면 됩니다.**

```
업데이트 전으로 되돌려줘
```

파트너가 바꾼 파일은 **권한 목록 하나**뿐이고, 그 원본을 `.claude/settings.json.bak` 에
남겨 두었습니다. 되돌리기는 그 파일 하나면 끝납니다.

`CLAUDE.md` 처럼 새 판으로 바뀐 문서도 **이전 판 원본이 폴더 안에 함께 들어 있어서**,
"예전 문구로 돌려줘" 라고 하시면 파트너가 찾아 넣어 드립니다.

---

## 5. 막히면

| 증상 | 원인 · 해결 |
|---|---|
| `파트너 폴더가 아니에요` 라고 나와요 | 다른 폴더에서 실행한 것입니다. `cd` 로 파트너 폴더에 간 뒤 다시 하세요. **아무것도 바뀌지 않았으니 안심하셔도 됩니다.** |
| "업데이트 마무리해줘" 를 파트너가 못 알아들어요 | **2단계(껐다 켜기)를 안 한 것입니다.** 열에 아홉은 이겁니다. claude 를 껐다 켜고 다시 말해 보세요. |
| 중간에 멈췄어요 / 인터넷이 끊겼어요 | **1단계 그 한 줄을 그대로 다시** 실행하면 됩니다. 여러 번 해도 안전합니다. |
| 대시보드 화면이 이상해요 | 열려 있는 대시보드 터미널 창을 `Ctrl+C` 로 끄고 `./dashboard.sh` 를 다시 실행하세요. 브라우저도 ⌘R 로 새로고침. |
| 기록이 없어진 것 같아요 | `notes/` 폴더를 Finder 로 직접 열어 확인해 보세요. 업데이트는 이 폴더를 건드리지 않습니다. 그래도 이상하면 **4절로 되돌리고** 알려 주세요. |
| 텔레그램이 조용해요 | 업데이트와 무관할 가능성이 큽니다. **받는 창이 꺼진 것**부터 확인하세요 (`docs/GUIDE.md` 5절). |

> 그래도 안 되면 **되돌리는 게 먼저입니다**(4절). 원상복구해 두고 단톡방에 남겨 주세요.
> 작동하던 걸 놓치고 붙잡고 있는 것보다, 돌려놓고 물어보는 편이 항상 빠릅니다.

---

## 6. 업데이트 뒤에 해 볼 것

새 판을 받았다고 해서 **뭔가 달라진 게 바로 보이지는 않습니다.** 새로 되는 걸 한 번 써 봐야
비로소 손에 잡힙니다. 그래서 3단계에서 파트너가 **지금 상태에 맞는 것만 골라** 두세 줄로 알려 줍니다.

아래는 그 전체 목록입니다. **내 경우에 해당하는 줄만** 보시면 됩니다.
문장은 그대로 복사해서 파트너에게 보내셔도 됩니다.

### 구글 캘린더를 **연결해 두신 경우**

이번 판에서 제일 크게 달라지는 자리입니다. 화면에서 누르지 않고 **말로** 됩니다.

```
내일 일정 브리핑해줘
```
```
금요일 2시에 미팅 하나 잡아줘
```
```
3시 미팅 4시로 옮겨줘
```
```
그 일정 취소해줘
```

> 지운 일정은 **되돌릴 수 있습니다.** 지운 직후에 "되돌려줘" 라고 하시면 됩니다.
> 공휴일처럼 **공유받은 캘린더**는 못 고칩니다. 구글이 막는 것이라, 파트너가 몰래 다른 곳에
> 만들지 않고 그대로 알려 줍니다.

### 구글 캘린더를 **아직 안 붙이신 경우**

위의 일정 조율은 **캘린더를 붙여야 됩니다.** 붙이는 도중에 막히던 문제도 이번에 고쳤습니다.

```
캘린더 연결해줘
```

> **안 붙이셔도 됩니다.** 일정을 많이 챙기지 않는 분도 있고, 다른 데서 이미 잘 굴러가는 분도
> 있습니다. 그런 경우 `notes/calendar-fallback.md` 에 이번 주 일정만 몇 줄 적어 두셔도
> 브리핑은 그대로 받습니다. 다만 **파트너가 일정을 고치지는 못합니다** — 그때는 파트너가
> "제가 일정을 바꾸지는 못해요" 라고 사실대로 말합니다.

### **화면(대시보드)을 쓰시는 경우**

```
./dashboard.sh
```

> 열리면 **오른쪽 아래 말풍선**을 눌러 보세요. 전에 나눈 대화가 그대로 있습니다.
> 지금까지는 늘 비어 있었는데, 저희 쪽 문제였습니다.

### **모두에게 — 없는 기능은 만들어 붙일 수 있습니다**

이건 v1 에는 없던 것입니다. 이 파트너는 완성품이 아니라 **뼈대**라서, 반복되는 일이 생기면
그때 하나씩 붙이면 됩니다.

```
매주 월요일에 하는 ○○, 스킬로 만들어줘
```

> 무엇을 반복하는지 몇 가지 물어보고, 그 자리에서 만들어 한 번 시험까지 해 봅니다.
> 단, **못 하는 일은 만들어도 안 됩니다** — 메일·카톡 자동 발송, 외부 서비스 로그인,
> 이 폴더 밖 건드리기. 파트너가 된다고 하지 않습니다.

### **기록이 아직 얼마 없으신 경우**

새 기능보다 이게 먼저입니다. 기록이 없으면 회상도 초안도 전부 뻔한 답이 됩니다.

```
_import 폴더에 넣어뒀어, 정리해줘
```

> 카톡 내보낸 텍스트, 노션에서 뽑은 파일, 회의록 PDF, 화이트보드 사진 다 됩니다.
> (한글 `.hwp` 만 못 읽습니다 — PDF 로 내보내 주세요.) **원본은 지우지 않습니다.**

---

### 안 쓰셔도 됩니다

새로 생긴 것이 **내 일과 상관없을 수 있습니다.** 그건 정상입니다.

- 캘린더를 안 쓰시면 안 붙이셔도 됩니다.
- 화면을 안 여시면 안 여셔도 됩니다. 텔레그램만으로도 그대로 굴러갑니다.
- 스킬을 안 만드셔도 됩니다. 반복이 생기기 전에는 만들 이유가 없습니다.

**업데이트는 쓰던 방식을 바꾸라는 통보가 아닙니다.** 이번 판에서 정말 중요한 건 **고친 것**이고,
그건 이미 조용히 적용됐습니다. 아무것도 안 하셔도 어제보다 나아진 상태입니다.

파트너가 뭔가를 권했는데 지금 필요 없으면 **"안 쓸래요"** 라고 하시면 됩니다. 거기서 끝납니다.
