.DS_Store
install.log
run*.log
out.log
.omc/
.dash-viewer.json

# 들여온 원본 — 카톡·노션에서 가져온 개인 기록이다. 절대 커밋하지 않는다
_import/

# 런타임 기록 — 대시보드가 쓰는 사용자별 데이터. 제품이 아니다
# (install.sh 가 만드는 건 status.json·team.json 둘뿐이다)
state/activity.json
state/automations.json

# 사용자가 "항상 허용"을 눌러 쌓인 개인 권한 목록 — 제품이 아니므로 미러로 내보내지 않는다
.claude/settings.local.json

# update 스킬이 마무리를 끝내고 남기는 기록(적용된 매니페스트 사본) — 각 컴퓨터의 런타임 산출물이다
.product/installed.json

# 구글 캘린더 연결 — 개인 열쇠·브라우저 프로필·설치물은 절대 커밋하지 않는다
state/google-calendar.json
tools/google-connect/node_modules/
tools/google-connect/.chrome-profile/
tools/google-connect/bun.lock*
